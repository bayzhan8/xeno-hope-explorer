# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files are shared with collaborators out of band, not via git.
# .env.example stays tracked as the documented template.
.env
.env.*
!.env.example

# Dev server pid file (written by local dev wrapper, never source)
.dev_pid

# TypeScript incremental build cache (regenerated on every `tsc -b`)
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

public/viz_data/*.json